{
  "name": "starter",
  "version": "1.0.0",
  "description": "Landing page for natours",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "node-sass": "^4.11.0"
  }
}
